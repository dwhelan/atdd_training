# DRY up the add feature
It is important to keep the features and step definitions DRY by removing duplication.

You may have noticed that the `Given` statement is the same for all the scenarios.

DRY it up using the `Background` gherkin statement.
